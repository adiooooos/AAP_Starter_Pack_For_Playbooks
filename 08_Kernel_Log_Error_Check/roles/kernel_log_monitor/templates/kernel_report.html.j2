<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kernel Error Monitoring Report - {{ inventory_hostname }}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            margin: 20px;
            background-color: #f4f7f6;
            color: #333;
            line-height: 1.6;
        }
        .container {
            width: 80%;
            max-width: 1200px;
            margin: auto;
            background: white;
            padding: 30px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            border-radius: 8px;
        }
        .header {
            border-bottom: 2px solid #ecf0f1;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        .header h1 {
            color: #2c3e50;
            margin: 0 0 10px 0;
            font-size: 2em;
        }
        .hostname, .report-time {
            color: #555;
            font-size: 0.9em;
            margin: 5px 0;
        }
        .status {
            padding: 20px;
            margin-top: 20px;
            font-size: 1.2em;
            text-align: center;
            border-radius: 5px;
            font-weight: bold;
        }
        .status-ok {
            background-color: #e8f5e9;
            color: #2e7d32;
            border: 2px solid #4caf50;
        }
        .status-error {
            background-color: #ffebee;
            color: #c62828;
            border: 2px solid #f44336;
        }
        .error-section {
            margin-top: 30px;
            padding: 20px;
            background-color: #fafafa;
            border-radius: 5px;
            border-left: 4px solid #e67e22;
        }
        .error-section h2 {
            color: #c0392b;
            margin-top: 0;
            font-size: 1.5em;
            border-bottom: 2px solid #c0392b;
            padding-bottom: 10px;
        }
        .error-section h3 {
            color: #e67e22;
            margin-top: 0;
            font-size: 1.3em;
            border-bottom: 2px solid #e67e22;
            padding-bottom: 10px;
        }
        .error-list {
            list-style: none;
            padding: 0;
            margin: 20px 0 0 0;
        }
        .error-list li {
            background: #fdfefe;
            border: 1px solid #eee;
            padding: 15px;
            margin-bottom: 10px;
            font-family: 'Courier New', Courier, monospace;
            word-wrap: break-word;
            border-radius: 4px;
            border-left: 3px solid #3498db;
        }
        .error-list li:hover {
            background-color: #f0f0f0;
        }
        .info-box {
            background-color: #e8f4f8;
            border-left: 4px solid #3498db;
            padding: 15px;
            margin: 20px 0;
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Kernel Error Monitoring Report</h1>
            <div class="hostname">Host: {{ inventory_hostname }}</div>
            <div class="report-time">Report Generation Time: {{ ansible_date_time.iso8601 }}</div>
        </div>

        {% if unique_errors is defined and unique_errors | length > 0 %}
            <div class="status status-error">
                ⚠️ Kernel-level errors detected! Please investigate immediately!
            </div>

            {% if detected_errors.critical is defined and detected_errors.critical | length > 0 %}
            <div class="error-section">
                <h2>Critical Errors ({{ detected_errors.critical | length }})</h2>
                <p class="info-box">
                    <strong>ℹ️ Note:</strong> Critical errors trigger immediate wall broadcast alerts to all logged-in users on this host.
                </p>
                <ul class="error-list">
                    {% for error in detected_errors.critical %}
                    <li>{{ error }}</li>
                    {% endfor %}
                </ul>
            </div>
            {% endif %}

            {% if detected_errors.severe is defined and detected_errors.severe | length > 0 %}
            <div class="error-section">
                <h3>Severe Warnings ({{ detected_errors.severe | length }})</h3>
                <p class="info-box">
                    <strong>ℹ️ Note:</strong> Severe errors are logged to {{ kernel_events_log }} for later analysis.
                </p>
                <ul class="error-list">
                    {% for error in detected_errors.severe %}
                    <li>{{ error }}</li>
                    {% endfor %}
                </ul>
            </div>
            {% endif %}

        {% else %}
            <div class="status status-ok">
                ✅ System status is healthy. No kernel errors detected.
            </div>
            <div class="info-box" style="margin-top: 20px;">
                <strong>📋 Summary:</strong>
                <ul>
                    <li>No critical or severe kernel errors were found in the recent kernel logs</li>
                    <li>This report is generated by Ansible kernel_log_monitor role</li>
                    <li>Regular monitoring helps identify potential issues before they cause system failures</li>
                </ul>
            </div>
        {% endif %}

        <div class="info-box" style="margin-top: 30px;">
            <strong>📋 Report Information:</strong>
            <ul>
                <li>This report is generated by Ansible kernel_log_monitor role</li>
                <li>Log source: {% if ansible_distribution_major_version | int >= 8 %}journalctl --dmesg{% else %}/var/log/messages{% endif %}</li>
                <li>Log lines analyzed: {{ log_lines | default(100) }}</li>
                <li>Operating System: {{ ansible_distribution }} {{ ansible_distribution_major_version }}</li>
            </ul>
        </div>
    </div>
</body>
</html>

